# C-VS
C语言的学习
//2021年7月12日 
//下标引用，函数调用和结构成员 
//
//		[]		()		.		-> 
//
//
//常见的关键字 
//
//		auto
//我们在主函数main中，定义了一个变量，这个变量是局部变量，它会在出了花括号后自动销毁，所以局部变量也称为自动变量 
int main()
{ int a=30;//局部变量-自动变量 前面有一个auto关键字，所以通常省略掉了。 
	
	
	return 0;
 } 

	
//		break  //两个用处，在循环语句中用来停止循环，在switchcase语句中	也会用到 
//		case
//		char	字符类型 
//		const		常变量 
//		continue	继续 
//		default		默认	 
//		do			常用在do...while循环 
//		double		双精度浮点型 默认输出 6 位小数(不足六位以 0 补齐,超过六位按四舍五入截断),可以根据自己的需要去修改输出的位数,例如输出两位小数:(%2lf)
//		else		
//		enum		枚举 
//		extern		引入外部符号 
//		float		单精度浮点型 
//		int			整型 
//		short		短整型 
//		long		长整型 
//		for			循环语句 
//		goto		程序跳转语句 
//		if			条件语句 
//		register	 寄存器关键字
//计算机要存储数据的话，数据一般存到硬盘，内存，高速缓冲存储器（cache），寄存器。 
//访问速度：寄存器>cache>内存>硬盘（访问顺序与访问速度大小的顺序相同） 
int main()
{
	register int a=10;//如果在程序中，a的使用次数非常多，那么我们可以将a定义成寄存器的整型变量，使得程序对a的读取速度更快。  
//	但这里的register仅仅只是建议，因为寄存器的空间是很小且有限的，当寄存器变量多时，由编译器自行判断把哪个先定义成寄存器变量 
	return 0;
	
 } 
//		return		返回 
//		signed		有符号数
//我们平常用的int定义的变量可以是有符号的，事实上int的全称是signed int，前面的signed被省略了
//		unsigned
//而要定义一个无符号数，就是unsigned int
//例如 unsigned int a=10; //这里的a永远都是正的，	并且不存在符号位，第一位永远被解释为正数（二进制）	，即使放进一个负数，它也会编译为正数  

//		sizeof
//		static
//		struct		结构体关键字 
//		switch		
//		typedef		类型定义 
//		union		联合体，或者叫共用体 
//		void		空类型 
//		volatile	用的比较少 
//		while		循环 

//以上这些关键字名称，都不能拿来当符号名定义！！ 
